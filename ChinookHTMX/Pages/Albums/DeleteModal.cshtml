@model ChinookHTMX.Pages.Albums.DeleteModel

<div id="delete-album-modal" class="modal fade" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">
                    <i class="fas fa-exclamation-triangle me-2"></i>
                    Confirm Deletion
                </h5>
                <button type="button" class="btn-close btn-close-white"
                        data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="alert alert-warning">
                    <strong>Warning!</strong> This action cannot be undone.
                </div>

                <p class="mb-3">Are you sure you want to delete this album?</p>

                <!-- Entity Details -->
                <div class="card bg-light border-left-danger">
                    <div class="card-body">
                        <dl class="row mb-0">
                            <dt class="col-sm-3">Album Title:</dt>
                            <dd class="col-sm-9">
                                <strong>@Html.DisplayFor(model => model.Album.Title)</strong>
                            </dd>
                            @if (Model.Album.Artist != null)
                            {
                                <dt class="col-sm-3">Artist:</dt>
                                <dd class="col-sm-9">
                                    @Html.DisplayFor(model => model.Album.Artist.Name)
                                </dd>
                            }
                        </dl>
                    </div>
                </div>

                <!-- Results Container -->
                <div id="delete-results" class="mt-3"></div>
            </div>
            <div class="modal-footer">
                <form method="post"
                      hx-post="@Url.Page("Delete", values: new { Model.Album.Id })"
                      hx-target="#delete-results"
                      hx-swap="innerHTML"
                      hx-indicator="#delete-loading"
                      hx-on::after-request="if(event.detail.successful) {
                          setTimeout(() => {
                              bootstrap.Modal.getInstance(document.getElementById('delete-album-modal')).hide();
                              htmx.trigger('body', 'refreshTable');
                          }, 1500);
                      }">

                    <input type="hidden" asp-for="Album.Id"/>
                    @Html.AntiForgeryToken()

                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                        <i class="fas fa-times"></i> Cancel
                    </button>
                    <button type="submit" class="btn btn-danger">
                        <span id="delete-loading" class="htmx-indicator">
                            <span class="spinner-border spinner-border-sm me-2"></span>
                        </span>
                        <i class="fas fa-trash"></i> Delete Album
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    function showModal() {
        const modal = new bootstrap.Modal('#delete-album-modal');
        modal.show();
    }
    // scopes the modal so we can keep creating them
    showModal();
</script>